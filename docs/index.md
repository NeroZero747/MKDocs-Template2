# Ultimate Material Playground for ETL Docs

This project showcases a wide sweep of Material features so you can decide what to keep.

- Start at **Playground → Overview**
- See **UI Components**, **Layouts & Grids**, **Data Tables**, and **Dashboards**
- API docs come from `src/etl_example` using **mkdocstrings**

```bash
pip install -r requirements.txt
mkdocs serve
```
